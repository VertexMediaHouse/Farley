// Estimate Page – placeholder
// Previously the file was empty causing import errors.
// This component simply renders the PriceEstimatorPage so the existing route works.

import React from 'react';
import PriceEstimatorPage from './PriceEstimatorPage';

const EstimatePage: React.FC = () => {
  return <PriceEstimatorPage />;
};

export default EstimatePage;
